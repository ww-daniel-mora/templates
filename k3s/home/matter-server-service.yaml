apiVersion: v1
kind: Service
metadata:
  name: matter-server
  namespace: home
spec:
  type: ClusterIP
  selector:
    app: matter-server
  ports:
    - name: ws
      port: 5580
      targetPort: 5580
