# TEMPLATE ONLY — do not commit actual secret value.
# Create the real secret with:
#   k3s kubectl create secret generic portainer-agent-secret \
#     -n portainer \
#     --from-literal=AGENT_SECRET=<generate with: openssl rand -hex 32>
apiVersion: v1
kind: Secret
metadata:
  name: portainer-agent-secret
  namespace: portainer
type: Opaque
# data:
#   AGENT_SECRET: <base64-encoded value>
